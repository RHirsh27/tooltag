{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install && cd apps/tooltag && npx prisma generate && pnpm build",
  "devCommand": "cd apps/tooltag && pnpm dev",
  "installCommand": "echo 'Skipping install, handled in buildCommand'",
  "outputDirectory": "apps/tooltag/build",
  "framework": null
}

